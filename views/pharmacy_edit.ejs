
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>Edit Medicine — Ayuvaani</title>
  <style>
    :root{
      --brand:#0b5aa6;
      --accent:#e6eef8;
      --muted:#6b6f76;
      --card-bg:#ffffff;
      --radius:12px;
      --gap:18px;
      font-family:"Segoe UI", Roboto, "Helvetica Neue", Arial, sans-serif;
      color:var(--muted);
    }

    body{
      background:#f4f6f8;
      margin:0;
      padding:0;
    }

    /* Navbar */
    nav{
      width:100%;
      background-color:var(--brand);
      color:#fff;
      display:flex;
      justify-content:space-between;
      align-items:center;
      padding:12px 24px;
      position:sticky;
      top:0;
      z-index:1000;
      box-shadow:0 4px 8px rgba(0,0,0,0.1);
    }
    nav .logo{
      font-size:1.5rem;
      font-weight:bold;
    }
    nav a{
      color:#fff;
      text-decoration:none;
      margin-left:1.5rem;
      font-weight:500;
    }
    nav a:hover{
      text-decoration:underline;
    }

    .wrap{
      max-width:900px;
      margin:36px auto;
      padding:20px;
    }

    /* Header */
    .header{
      display:flex;
      align-items:center;
      gap:16px;
      background:linear-gradient(90deg,var(--accent),#fff);
      padding:18px;
      border-radius:14px;
      box-shadow:0 6px 18px rgba(11,90,166,0.08);
      border-left:6px solid var(--brand);
      margin-bottom:20px;
    }
    .crest{
      width:64px;
      height:64px;
      border-radius:8px;
      display:flex;
      align-items:center;
      justify-content:center;
      background:var(--brand);
      color:#fff;
      font-weight:700;
      font-size:18px;
    }
    .title-block{display:flex;flex-direction:column}
    h1{margin:0;font-size:20px;color:var(--brand)}
    .sub{font-size:13px;color:var(--muted);margin-top:4px}

    /* Card */
    .card{
      background:var(--card-bg);
      padding:30px;
      border-radius:var(--radius);
      box-shadow:0 6px 20px rgba(20,24,28,0.05);
    }

    h2{margin:0 0 20px 0;color:var(--brand);font-size:22px}

    form{
      display:flex;
      flex-direction:column;
      gap:var(--gap);
    }

    fieldset{
      border:1px solid #cbd5e1;
      border-radius:8px;
      padding:16px;
      background:#fafbfd;
    }
    fieldset legend{
      font-weight:600;
      color:var(--brand);
      padding:0 6px;
    }

    label{
      font-size:14px;
      color:#444;
      margin-bottom:4px;
      font-weight:500;
    }

    input{
      padding:12px 14px;
      border:1px solid #cbd5e1;
      border-radius:8px;
      font-size:1rem;
      transition:0.3s;
      width:100%;
      box-sizing:border-box;
    }
    input:focus{
      border-color:var(--brand);
      box-shadow:0 0 6px rgba(11,90,166,0.3);
      outline:none;
    }

    button{
      background-color:var(--brand);
      color:#fff;
      padding:12px;
      border:none;
      border-radius:8px;
      font-size:1rem;
      font-weight:600;
      cursor:pointer;
      transition:0.3s;
    }
    button:hover{
      background-color:#074080;
      transform:translateY(-2px);
    }

    /* Stock & Price styles */
    .stock-price{
      display:flex;
      gap:12px;
      flex-wrap:wrap;
    }
    .stock-price > div{
      flex:1 1 200px; /* Minimum width 200px and flexible growth */
      display:flex;
      flex-direction:column;
    }

    @media(max-width:600px){
      .wrap{padding:12px}
      .header{flex-direction:column;align-items:flex-start}
      .stock-price{flex-direction:column}
    }
  </style>
</head>
<body>
  <!-- Navbar -->
  <nav>
    <div class="logo">Ayuvaani</div>
    <div class="nav-links">
      <a href="/">Home</a>
      <a href="/pharmacy/dashboard">Dashboard</a>
      <a href="/pharmacy/login">Logout</a>
    </div>
  </nav>

  <div class="wrap">
    <!-- Page Header -->
    <header class="header">
      <div class="crest">GOV</div>
      <div class="title-block">
        <h1>Edit Medicine</h1>
        <div class="sub">Ayuvaani Pharmacy Portal — Update medicine details</div>
      </div>
    </header>

    <!-- Form Card -->
    <section class="card">
      <h2>Medicine Information</h2>
      <form method="POST" action="/pharmacy/edit/<%= medicine._id %>">
        <!-- Basic Info -->
        <fieldset>
          <legend>Basic Information</legend>
          <label>Medicine Name</label>
          <input type="text" name="name" value="<%= medicine.name %>" placeholder="Medicine Name" required />
          
          <label>Manufacturer</label>
          <input type="text" name="manufacturer" value="<%= medicine.manufacturer %>" placeholder="Manufacturer" required />
          
          <label>Batch Number</label>
          <input type="text" name="batchNumber" value="<%= medicine.batchNumber %>" placeholder="Batch Number" required />
        </fieldset>

        <!-- Dates -->
        <fieldset>
          <legend>Dates</legend>
          <label>Manufacturing Date</label>
          <input type="date" name="manufacturingDate" value="<%= medicine.manufacturingDate.toISOString().split('T')[0] %>" required />

          <label>Expiry Date</label>
          <input type="date" name="expiryDate" value="<%= medicine.expiryDate.toISOString().split('T')[0] %>" required />
        </fieldset>

        <!-- Stock & Pricing -->
        <fieldset>
          <legend>Stock & Pricing</legend>
          <div class="stock-price">
            <div>
              <label>Stock Quantity</label>
              <input type="number" name="stock" value="<%= medicine.stock %>" placeholder="Stock Quantity" required />
            </div>
            <div>
              <label>Price per Unit (₹)</label>
              <input type="number" name="price" value="<%= medicine.price %>" placeholder="Price per Unit" required />
            </div>
          </div>
        </fieldset>

        <button type="submit">Update Medicine</button>
      </form>
    </section>
  </div>
</body>
</html>
