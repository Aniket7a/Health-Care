<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Pharmacy Login — Ayuvaani</title>
  <style>
    :root {
      --brand: #0b5aa6;
      --brand-gradient: linear-gradient(135deg, #0b5aa6, #1c7ed6);
      --accent: #f0f4f8;
      --muted: #555;
      --input-bg: #fff;
      --input-border: #cfd8dc;
      --card-radius: 1rem;
    }

    * { box-sizing: border-box; margin: 0; padding: 0; font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif; }

    body {
      min-height: 100vh;
      display: flex;
      flex-direction: column;
      background: var(--accent);
    }

    nav {
      width: 100%;
      padding: 1rem 2rem;
      background: var(--brand);
      display: flex;
      align-items: center;
      justify-content: space-between;
      color: #fff;
      box-shadow: 0 4px 8px rgba(0,0,0,0.1);
    }

    nav .logo {
      font-size: 1.5rem;
      font-weight: bold;
    }

    nav a {
      color: #fff;
      text-decoration: none;
      margin-left: 1.5rem;
      font-weight: 500;
    }

    nav a:hover {
      text-decoration: underline;
    }

    /* Slogan header */
    .slogan-header {
      text-align: center;
      background: var(--brand-gradient);
      color: #fff;
      padding: 1rem 2rem;
      font-size: 1.2rem;
      font-weight: 500;
      letter-spacing: 0.5px;
    }

    .container {
      flex: 1;
      display: flex;
      justify-content: center;
      align-items: center;
      padding: 2rem;
      gap: 2rem;
      flex-wrap: wrap;
    }

    /* Login Card */
    .login-card {
      background: #fff;
      padding: 2.5rem 3rem;
      border-radius: var(--card-radius);
      box-shadow: 0 12px 25px rgba(0,0,0,0.15);
      width: 100%;
      max-width: 400px;
      text-align: center;
      position: relative;
      overflow: hidden;
      flex: 1 1 350px;
    }

    .login-card::before {
      content: '';
      position: absolute;
      top: -60px;
      left: -60px;
      width: 150px;
      height: 150px;
      background: var(--brand-gradient);
      border-radius: 50%;
      opacity: 0.2;
    }

    .login-card h2 {
      color: var(--brand);
      margin-bottom: 1rem;
      font-size: 2rem;
      letter-spacing: 1px;
    }

    form {
      display: flex;
      flex-direction: column;
      gap: 1.3rem;
      position: relative;
      z-index: 1;
    }

    input {
      width: 100%;
      padding: 0.9rem 1rem;
      border-radius: 0.8rem;
      border: 1px solid var(--input-border);
      background: var(--input-bg);
      font-size: 1rem;
      transition: border 0.3s, box-shadow 0.3s;
    }

    input:focus {
      outline: none;
      border-color: var(--brand);
      box-shadow: 0 0 5px rgba(11, 90, 166, 0.3);
    }

    button {
      padding: 0.9rem 1rem;
      border: none;
      border-radius: 0.8rem;
      background: var(--brand);
      color: #fff;
      font-size: 1rem;
      cursor: pointer;
      transition: background 0.3s, transform 0.2s;
    }

    button:hover {
      background: #09437a;
      transform: translateY(-2px);
    }

    .footer {
      margin-top: 1rem;
      font-size: 0.9rem;
      color: var(--muted);
    }

    .footer a {
      color: var(--brand);
      text-decoration: none;
    }

    .footer a:hover {
      text-decoration: underline;
    }

    /* Empowerment Image */
    .empowerment-image {
      flex: 1 1 350px;
      display: flex;
      justify-content: center;
      align-items: center;
      max-width: 600px;
    }

    .empowerment-image img {
      width: 100%;
      border-radius: var(--card-radius);
      box-shadow: 0 8px 20px rgba(0,0,0,0.1);
    }

    @media(max-width: 900px){
      .container {
        flex-direction: column-reverse;
        gap: 1.5rem;
      }
    }
  </style>
</head>
<body>
  <nav>
    <div class="logo">Ayuvaani</div>
    <div class="nav-links">
      <a href="/">Home</a>
      <a href="/pharmacy/signup">Sign Up</a>
    </div>
  </nav>

  <!-- Slogan Header -->
  <div class="slogan-header">
    Secure Access to Your Pharmacy Data — Empowering Indian Healthcare
  </div>

  <div class="container">
    <!-- Login Form Left -->
    <div class="login-card">
      <h2>Pharmacy Login</h2>
      <form method="POST" action="/pharmacy/login">
        <input type="email" name="email" placeholder="Email" required />
        <input type="password" name="password" placeholder="Password" required />
        <button type="submit">Login</button>
      </form>
      <div class="footer">
        Don't have an account? <a href="/pharmacy/signup">Sign up</a>
      </div>
    </div>

    <!-- Image Right -->
    <div class="empowerment-image">
      <img src="https://media.istockphoto.com/id/1390982483/photo/smiling-pharmacis-giving-medicine-cover-to-customer-at-pharmacy-shop-concept-of-customer.jpg?s=1024x1024&w=is&k=20&c=vafDGi4CapyLqgAF3Whkw5NdTxEKjDKa3xw5VW5z7qg=" alt="Empowering Indian Healthcare">
    </div>
  </div>
</body>
</html>
